# Movie backgrounds

Movie scene art behind the personality character, named by 4-letter type code:
`public/backgrounds/<TYPE>.jpg` (landscape, cropped to fill the hero panel).

See `public/characters/README.md` for the full type → character mapping.
